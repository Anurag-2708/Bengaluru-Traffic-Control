services:
  - type: web
    name: bengaluru-traffic-backend
    env: docker
    plan: free
    branch: deployment
    dockerfilePath: Dockerfile
    port: 8000
    envVars:
      - key: GEMINI_API_KEY
        sync: false
