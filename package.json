{
  "name": "bengaluru-traffic-control-root",
  "version": "1.0.0",
  "description": "Bengaluru Traffic Control Dashboard",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install-frontend": "npm install --prefix frontend"
  }
}
